# Простая инструкция - что делать

## Шаг 1: Дождитесь готовности Redis базы

На первом скриншоте видно что Redis база "redis-emerald-house" еще **Initializing** (инициализируется).

**Что делать:**
1. Подождите 1-2 минуты
2. Обновите страницу (F5)
3. Когда статус изменится на "Ready" или "Active" - переходите к шагу 2

## Шаг 2: Скопируйте REDIS_URL

Когда база будет готова:

1. На странице Redis базы найдите раздел **Quickstart**
2. Откройте вкладку **`.env.local`**
3. Там будет строка типа: `REDIS_URL="redis://..."` (НЕ `database_provisioning_in_progress`)
4. Скопируйте весь URL (включая кавычки не нужно, только сам URL)

**Пример:** `redis://default:password123@redis-host:6379`

## Шаг 3: Добавьте REDIS_URL в Vercel

1. Зайдите в ваш проект на **Vercel**
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **Add Environment Variable**
4. Заполните:
   - **Key:** `REDIS_URL`
   - **Value:** вставьте скопированный URL из шага 2
   - **Environment:** выберите Production, Preview, Development (или все)
5. Нажмите **Save**

## Шаг 4: Пересоберите проект

После добавления переменной:

1. В Vercel перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (...)
4. Выберите **Redeploy**

Или просто сделайте новый коммит и пуш в git.

## Шаг 5: Протестируйте

После пересборки отправьте боту в Telegram:

```
/changedomain createsync.click
```

Бот должен ответить что домен изменен, и это произойдет **мгновенно** без пересборки!

## Проверка

Проверьте текущий домен:

```
/domain
```

---

## Если что-то не работает

### Redis все еще "Initializing"
- Подождите еще немного (может занять до 5 минут)
- Обновите страницу

### Не могу найти REDIS_URL
- На странице Redis базы найдите кнопку **"Connect Project"** или **"Open in Redis"**
- Там должен быть раздел с URL для подключения

### Ошибка в боте "Redis not configured"
- Проверьте что переменная `REDIS_URL` добавлена в Vercel
- Убедитесь что пересобрали проект после добавления переменной
- Проверьте что URL правильный (начинается с `redis://`)

### Домен не меняется
- Проверьте логи в Vercel (Deployments → последний деплой → Logs)
- Убедитесь что Redis база работает (статус "Ready")

---

## Краткая версия (TL;DR)

1. ⏳ Дождитесь готовности Redis (статус "Ready")
2. 📋 Скопируйте REDIS_URL из Quickstart
3. ⚙️ Добавьте REDIS_URL в Vercel → Settings → Environment Variables
4. 🔄 Пересоберите проект (Redeploy)
5. ✅ Протестируйте: `/changedomain createsync.click`

Готово! 🎉
