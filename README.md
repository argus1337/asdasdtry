# CreateSync - Influencer Marketing Agency Website

Современный многостраничный сайт для агентства influencer marketing, построенный на Next.js 16 с использованием TypeScript и Tailwind CSS.

## 🚀 Быстрый старт

### Требования

- Node.js 18+ (проверьте версию: `node --version`)
- pnpm (рекомендуется) или npm/yarn

### Установка и запуск

#### Вариант 1: С pnpm (рекомендуется)

1. **Установите pnpm глобально (если еще не установлен):**
   ```bash
   npm install -g pnpm
   ```

2. **Установите зависимости:**
   ```bash
   pnpm install
   ```

3. **Запустите dev сервер:**
   ```bash
   pnpm dev
   ```

4. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

#### Вариант 2: С npm

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите dev сервер:**
   ```bash
   npm run dev
   ```

3. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

### ⚠️ Важные замечания

- Если порт 3000 занят, Next.js автоматически предложит использовать следующий доступный порт (например, 3001)
- При первом запуске может потребоваться время на компиляцию
- Убедитесь, что у вас есть доступ в интернет для загрузки зависимостей

## 📁 Структура проекта

```
├── app/                    # Next.js App Router страницы
│   ├── about/             # Страница "О нас"
│   ├── clients/           # Страница клиентов
│   ├── creators/          # Страница для креаторов
│   ├── how-it-works/      # Как это работает (для брендов)
│   ├── services/          # Страницы сервисов
│   │   ├── tiktok/       # TikTok маркетинг
│   │   ├── instagram/    # Instagram маркетинг
│   │   └── youtube/      # YouTube маркетинг
│   ├── contact/          # Контакты (используется из главной)
│   ├── layout.tsx        # Главный layout
│   └── page.tsx          # Главная страница
├── components/            # React компоненты
│   ├── ui/               # UI компоненты (shadcn/ui)
│   ├── navbar.tsx        # Навигация
│   ├── footer.tsx        # Футер
│   └── ...               # Другие компоненты
├── lib/                  # Утилиты
└── public/               # Статические файлы
```

## 🎨 Страницы сайта

- **Главная** (`/`) - Лендинг с основными секциями
- **О нас** (`/about`) - История компании, команда, ценности
- **Как это работает** (`/how-it-works`) - Процесс работы для брендов
- **Для креаторов** (`/creators`) - Информация для инфлюенсеров
- **Клиенты** (`/clients`) - Список клиентов компании
- **Сервисы:**
  - TikTok (`/services/tiktok`)
  - Instagram (`/services/instagram`)
  - YouTube (`/services/youtube`)

## 🛠 Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **shadcn/ui** - UI компоненты
- **Lucide React** - Иконки
- **Radix UI** - Примитивы для UI компонентов

## 📝 Команды

```bash
# Разработка
pnpm dev

# Сборка для продакшена
pnpm build

# Запуск продакшен версии
pnpm start

# Линтинг
pnpm lint
```

## 🎯 Особенности

- ✅ Полностью адаптивный дизайн
- ✅ Современный UI/UX
- ✅ Оптимизация производительности
- ✅ SEO-friendly структура
- ✅ TypeScript для надежности кода
- ✅ Компонентный подход

## 📦 Установка зависимостей

Если у вас еще не установлен pnpm:

```bash
npm install -g pnpm
```

Или используйте npm:

```bash
npm install
```

## 🔧 Настройка

Все настройки находятся в:
- `next.config.mjs` - конфигурация Next.js
- `tailwind.config.js` - конфигурация Tailwind CSS
- `tsconfig.json` - конфигурация TypeScript

### 🤖 Настройка Telegram бота для уведомлений

Для получения уведомлений о новых заявках в Telegram:

1. **Создайте бота через @BotFather:**
   - Откройте Telegram и найдите @BotFather
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен бота

2. **Получите Chat ID:**
   - Найдите бота @userinfobot в Telegram
   - Отправьте ему любое сообщение
   - Он вернет ваш Chat ID (число вида `123456789`)

3. **Добавьте переменные окружения:**
   - Создайте файл `.env.local` в корне проекта
   - Добавьте следующие переменные:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен_бота
     TELEGRAM_CHAT_ID=ваш_chat_id
     ```

4. **Для Vercel:**
   - Перейдите в настройки проекта на Vercel
   - Откройте раздел "Environment Variables"
   - Добавьте `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`

После настройки при каждой отправке формы вы будете получать уведомление в Telegram с информацией:
- Канал (название и количество подписчиков)
- Email
- Имя
- IP адрес (если доступен)

## 📄 Лицензия

Private project

## 👥 Команда

CreateSync Team

---

**Примечание:** Убедитесь, что порт 3000 свободен перед запуском. Если порт занят, Next.js автоматически предложит использовать следующий доступный порт.

# zxczxc
